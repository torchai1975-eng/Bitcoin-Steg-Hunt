import HandCashConnectService from '../api/handcash_connect_service';
export default class HandCashOwner {
    handCashConnectService: HandCashConnectService;
    constructor(handCashConnectService: HandCashConnectService);
    static fromAuthToken(authToken: string, env?: {
        apiEndpoint: string;
        clientUrl: string;
        trustholderEndpoint: string;
    }, appSecret?: string, appId?: string): HandCashOwner;
    nextOwner(alias: string): Promise<string>;
    sign(rawTransaction: string, inputParents: unknown[], locks: unknown[]): Promise<string>;
    getNftLocations(): Promise<string[]>;
}
